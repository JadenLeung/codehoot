#include <stdio.h>
#include <string.h>
#include <assert.h>
#include <stdlib.h>
#include <limits.h>
#include <stdbool.h>
// last_digit(a): Gets the first digit of a
int last_digit(int a) {
    // Your code here
}

int main(void) {
    int a = 0;
    scanf("%d", &a);
    printf("%d\n", last_digit(a));
}
